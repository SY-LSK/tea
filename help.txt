Tea语言编译器 使用说明

基本命令格式：
tea [选项] <输入文件>

选项说明：
  -o <输出路径>  指定生成Python文件的位置（默认当前目录）
  -v            显示版本信息
  -h            查看帮助文档

使用示例：
  tea example.tea          # 编译example.tea文件
  tea demo.tea -o ./build  # 指定输出目录为build

注意：
输入文件必须使用.tea扩展名
输出将生成同名的.py可执行文件